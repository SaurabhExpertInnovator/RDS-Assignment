subset_list <- my_list[1]
print(subset_list)   # Outputs a list with the 'name' element

# Using double bracket [[ ]] - it returns the actual element, not a list
name_value <- my_list[[1]]
print(name_value) 
