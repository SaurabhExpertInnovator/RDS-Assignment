# Create a nested list
nested_list <- list(
  id = 1,
  info = list(
    numbers = c(10, 20, 30),
    words = c("apple", "banana", "cherry")
  )
)

# Print the nested list
print(nested_list)